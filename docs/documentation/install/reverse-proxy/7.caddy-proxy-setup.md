---
id: caddy
title: Caddy
---

import ReverseProxyInfo from "@site/src/components/ReverseProxyInfo";

<ReverseProxyInfo />

Add the following to your caddyfile.

```caddy
subdomain.domain.com {
    encode gzip zstd
    reverse_proxy <LOCAL_IP>:<PORT>
}
```
